extends layout

block content
  .col-sm-12(style='padding-left:0px;padding-right:0px')
    //a(href="/"): img(src="/images/error.jpg", alt="error_img" style='width:100%;height:400px;')
    h1= message
    h2= error.status
    if (error.stack)
    pre #{error.stack}
